"""Path planning utilities."""

from typing import List, Tuple


def plan_straight_line_path(start: Tuple[int, int], goal: Tuple[int, int]) -> List[Tuple[int, int]]:
    """Return a minimal placeholder path from start to goal."""
    return [start, goal]
