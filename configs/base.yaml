paths:
  raw_data: data/raw
  interim_data: data/interim
  processed_data: data/processed
  model_output: models/segmentation_model.txt
  figures_dir: reports/figures

training:
  epochs: 5
  learning_rate: 0.001
  batch_size: 8
  image_size: [256, 256]
  num_classes: 4
